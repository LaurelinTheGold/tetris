pub(crate) type BoardRow = u16;

// checks can piece no collision in new position

// sets board from piece when cannot move down

// Board has the board array, current piece, score, level,
// optional piece bag shuffled array of 4x 0-6 on the fly converted to a new piece
